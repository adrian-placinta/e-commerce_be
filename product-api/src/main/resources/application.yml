spring:
  application:
    name: "product-api"
  datasource:
    url: jdbc:mysql://localhost:3306/products
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    user: root
    password: admin
server:
  port: 8080


